import React, { useState } from "react";

const Username = () => {
  const [username, setUsername] = userState("");
  return (
    <div>
      <h1>Username</h1>
    </div>
  );
};

export default Username;
